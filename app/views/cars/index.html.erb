<h1>Our cars available</h1>

<%= link_to 'Add New Car', new_car_path, class: 'btn btn-primary' %>

<ul>
  <% @cars.each do |car| %>
    <li><%= link_to car.model, car_path(car) %></li>
  <% end %>
</ul>
